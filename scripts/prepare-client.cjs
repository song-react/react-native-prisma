#!/usr/bin/env node
const e=require("node:fs"),r=require("node:path");require("./patch-prisma-runtime.cjs");const i=r.resolve(process.argv[2]??"generated/prisma"),n=r.join(i,"client.ts"),t=r.join(i,"internal/class.ts");let a=e.readFileSync(n,"utf8");const o=a.replace(/import \* as process from 'node:process'\nimport \* as path from 'node:path'\nimport \{ fileURLToPath \} from 'node:url'\nglobalThis\['__dirname'\] = path\.dirname\(fileURLToPath\(import\.meta\.url\)\)/,"globalThis['__dirname'] = '/'");if(o===a&&!a.includes("globalThis['__dirname'] = '/'"))throw new Error(`Unsupported Prisma client: ${n}`);e.writeFileSync(n,o);let s=e.readFileSync(t,"utf8");if(s=s.replace('import { Buffer } from "buffer"\n',""),s.includes('from "@prisma/react-native/native"')||(s=s.replace('import * as runtime from "@prisma/client/runtime/client"','import * as runtime from "@prisma/client/runtime/client"\nimport { NativeQueryCompiler } from "@prisma/react-native/native"')),!s.includes("getNativeQueryCompiler")){const e=s.indexOf("function decodeBase64AsWasm"),r=s.lastIndexOf("\n",e)+1,i=s.indexOf("\n\n\nexport type ",r);if(e<0||i<0)throw new Error(`Unsupported Prisma client: ${t}`);s=s.slice(0,r)+"config.compilerWasm = {\n  getNativeQueryCompiler: async () => NativeQueryCompiler\n} as any"+s.slice(i)}if(!s.includes("getNativeQueryCompiler"))throw new Error(`Unsupported Prisma client: ${t}`);e.writeFileSync(t,s),console.log(`Prepared generated Prisma 7 client: ${i}`);